<form method="post,get" action="{{ path('adresse_delete', {'id': adresse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this adresse?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adresse.id) }}">
    <button class="btn">supprimer</button>
</form>
