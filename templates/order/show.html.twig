{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>
    {# {{ dump(detailCommande)}} #}

         <h4>Id commande:{{ commande.id }}</h4>
         <h4>Client : {{ user.nom|upper }} {{ user.prenom|capitalize }} </h4>
         <h4>Date commande: {{ commande.datetime|date }}</h4>
         
   
    <table class="table table-striped" style="margin :10x">
        <tbody>
            <tr>
                <th>Nom</th>
                <th>Prix</th>
                <th>Quantity</th>
                <th>Total</th>
             
            </tr>
           
        {% for detailCommande in detailCommandes %}
            <tr>
              
             
                <td>{{detailCommande.produit.nom }}</td>
                <td>{{detailCommande.produit.prix }}</td>
                <td>{{detailCommande.quantite }}</td>
                <td>{{detailCommande.produit.prix * detailCommande.quantite}}</td>
                

                
                </tr>
            {% endfor %} 
        </tbody>
    </table>

    {# <a href="{{ path('app_admin_produit_index') }}">back to list</a>

    <a href="{{ path('app_admin_produit_edit', {'id': produit.id}) }}">edit</a>

    {{ include('admin_produit/_delete_form.html.twig') }} #}

{% endblock %}
